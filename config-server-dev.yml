server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mhuancho/clean-config-api.git
          default-label: desa
          clone-on-start: true
          force-pull: true
      fail-fast: true
